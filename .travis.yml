language: python

matrix:
  include:
    - python: 3.4
      env: TOXENV=py34
    - python: 3.5
      env: TOXENV=py35
    - python: 3.6
      env: TOXENV=py36
    - python: 3.7
      env: TOXENV=py37
      sudo: required
      dist: xenial

install:
  - if [[ $TRAVIS_PYTHON_VERSION == 3.4 ]]; then
      pip install pip==9.0.3;
      pip run pip install pip==9.0.3;
    fi
  - pip install pipenv
  - pipenv install --dev

script:
  - pipenv run tox
